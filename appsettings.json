{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {

    // Aqui é o caminho para conexão do banco de dados.
    // Colocar certinho o caminho nos campos: Ex userid = root(Olha no banco de dados no inicio)

    "SalesWebMvcContext": "server=localhost;userid=root;password=12345678;database=saleswebmvcappdb"

  }
}